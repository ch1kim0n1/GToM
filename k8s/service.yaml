apiVersion: v1
kind: Service
metadata:
  name: gtom
  labels:
    app: gtom
    component: gstack
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
    name: http
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: health
  selector:
    app: gtom
